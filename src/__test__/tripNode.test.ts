describe('tripNode Creation', () => {
  it('should create a tripNode', async () => {
    // const tripNode = await createTripNode(tripNodePayload)
    // expect(tripNode).toEqual({
    //   ...tripNodePayload,
    //   _id: tripNode._id,
    //   createdAt: tripNode.createdAt,
    //   updatedAt: tripNode.updatedAt,
  })
})
